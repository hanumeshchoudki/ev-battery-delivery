# ನಮ್ಮ Charge - Package Requirements

## System Requirements
- Node.js >= 18.0.0
- npm >= 9.0.0 OR pnpm >= 8.0.0
- Git >= 2.0.0

## Frontend Dependencies (Root)

### Core Framework
next@14.2.16
react@18.3.1
react-dom@18.3.1

### UI Framework
tailwindcss@4.1.9
@radix-ui/react-*  (Multiple packages for UI components)

### HTTP & API
axios@^1.6.0
jwt-decode@^4.0.0

### Maps & Location
leaflet@^1.9.4
react-leaflet@^4.2.1
@types/leaflet@^1.9.8

### Fonts & Icons
geist@^1.3.0
lucide-react@latest

### Analytics
@vercel/analytics@^1.4.1

## Backend Dependencies (backend/)

### Core Framework
express@^4.18.2
dotenv@^16.3.1

### Database
@supabase/supabase-js@^2.39.0

### Authentication & Security
jsonwebtoken@^9.0.2
bcryptjs@^2.4.3
helmet@^7.1.0
cors@^2.8.5

### Utilities
morgan@^1.10.0  (HTTP request logger)

### Development
nodemon@^3.0.2  (Auto-restart on changes)

## Installation Commands

### Option 1: Using pnpm (Recommended - Faster)
```bash
# Frontend
pnpm install

# Backend
cd backend
npm install
```

### Option 2: Using npm
```bash
# Frontend
npm install

# Backend
cd backend
npm install
```

### Option 3: Using yarn
```bash
# Frontend
yarn install

# Backend
cd backend
yarn install
```

## Environment Variables Required

### Frontend (.env.local)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

### Backend (backend/.env)
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_KEY=your_supabase_service_role_key
JWT_SECRET=your_random_32_character_secret
PORT=5000
NODE_ENV=development

## Scripts Available

### Frontend
npm run dev          # Start development server
npm run build        # Build for production
npm run start        # Start production server
npm run lint         # Run ESLint

### Backend
npm run dev          # Start with nodemon (auto-reload)
npm start            # Start production server

## Port Usage
- Frontend: http://localhost:3000
- Backend API: http://localhost:5000

## Database Setup
Execute the SQL schema file:
backend/database/schema.sql

This creates:
- 4 tables (users, delivery_agents, orders, order_status_history)
- Triggers for auto-updates
- Views for analytics
- Sample test data

## Browser Compatibility
- Chrome/Edge >= 90
- Firefox >= 88
- Safari >= 14

## Development Tools Recommended
- VS Code with extensions:
  - ESLint
  - Prettier
  - Tailwind CSS IntelliSense
  - PostgreSQL (for Supabase)
